{
  "private": true,
  "scripts": {
    "build": "esbuild app/src/main.ts --bundle --minify --outdir=app/static --entry-names=bundle --format=iife --target=es2020 --loader:.woff2=file --loader:.woff=file",
    "dev": "esbuild app/src/main.ts --bundle --outdir=app/static --entry-names=bundle --format=iife --target=es2020 --loader:.woff2=file --loader:.woff=file --watch"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "fuse.js": "^7.1.0",
    "iconify-icon": "^3.0.2"
  }
}
